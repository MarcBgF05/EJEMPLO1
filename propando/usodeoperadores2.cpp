#include "iostream"
#include "cmath"
using namespace std;
int main()
{
    int a=12,b=5,resi, pot ;
    pot= pow(a,b); 
    resi=a%b;  
    cout<<"el residuo de la division es: "<<resi<<endl;  
    cout<<"el residuo de la division es: "<<pot<<endl;
    cout<<"el coceno de 0.8 es: "<<cos(0.8)<<endl; 
} 